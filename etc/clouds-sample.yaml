debug: false
# logFile: clouds.log
# 输出格式，可选值: table, table-light, json, yaml
format: table
language: en_US
# http 超时和重试
# httpTimeoutSecond: 0
# enableLogColor: false
# retryWaitTimeSecond: 2
# retryCount: 0

retryWaitTimeSecond: 1
retryCount: 5

# 云环境配置
clouds:
  default:
    region_name: RegionOne
    auth:
      auth_url: http://keystone.region1.dev:35357/v3
      project_domain_id: default
      user_domain_id: default
      project_name: admin
      username: admin
      password: PASSWORD
