default:
  serverNamePrefix: server-

flavor:
  id: test.m1.2g2v
  name: test.m1.2g2v
  vcpus: 2
  ram: 2048

server:
  name: test-server
  flavor:
    id: test.m1.2g2v
  # image:
  #   id: <image uuid>
  networks:
    # - uuid: <network uuid>
  availabilityZone:
  blockDeviceMappingV2:
   - bootIndex: 0
     uuid: <image uuid>
     volumeSize: 20
     sourceType: image
     destinationType: volume
    #  volumeType:
     deleteOnTermination: true
