debug: false
# 输出格式，可选值: table, table-light, json, yaml
format: table
language: en_US
# httpTimeout: 0

# 认证信息
auth:
  url: http://keystone-server:35357/v3
  region:
    id: RegionOne
  user:
    name: admin
    password: admin123
    domain:
      name: Default
  project:
    name: admin
    domain:
      name: Default

# iperf:
#   clientOptions: -t 60 -P 10
#   ServerOptions:
#   guestPath:
#   localPath:

test:
  availabilityZone:
  bootFromVolume: true
  flavors:
  images:
  networks:
