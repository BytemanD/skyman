debug: false
# 输出格式，可选值: default, table, table-light, json, yaml
format: default
language: en_US
# httpTimeout: 0

# 认证信息
auth:
  url: http://keystone-server:35357/v3
  region:
    name: RegionOne
  user:
    name: admin
    password: admin123
    domain:
      name: Default
  project:
    name: admin
    domain:
      name: Default

# 创建server时默认参数
server:
  flavor:
  image:
  network:
  volumeBoot: true
  volumeSize: 1
  availabilityZone:
  namePrefix: server
