debug: false

# 认证信息
auth:
  url: http://keystone-server:35357/v3
  regionName: RegionOne
  user:
    name: admin
    password: admin123
    domainName: Default
  project:
    name: admin
    domainName: Default

server:
  flavor:
  image:
  network:
  volumeBoot: true
  volumeSize: 1
  availabilityZone:
  namePrefix: server
